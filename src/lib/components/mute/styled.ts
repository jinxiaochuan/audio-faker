import styled from 'styled-components';

export const Mute = styled.div`
  padding: 0 15px;
  text-align: center;
  cursor: pointer;

  svg {
    font-size: 18px;
    color: rgba(0, 0, 0, 0.65);
  }
`;
