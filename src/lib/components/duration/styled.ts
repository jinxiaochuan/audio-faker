import styled from 'styled-components';

export const TimeWrap = styled.div`
  padding: 0 10px;
  margin-right: 15px;
  font-size: 14px;
  color: rgba(0, 0, 0, 0.65);
  flex-shrink: 0;
`;

export const TimeSplit = styled.span`
  padding: 0 5px;
`;
