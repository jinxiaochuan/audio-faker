import styled from 'styled-components';

export const Play = styled.div`
  margin-top: -3px;
  text-align: center;
  cursor: pointer;
  flex-shrink: 0;

  svg {
    font-size: 18px;
    color: rgba(0, 0, 0, 0.65);
  }
`;
